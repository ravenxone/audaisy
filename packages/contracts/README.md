# Audaisy Contracts

This package is the canonical shared contract surface for Audaisy.

- Backend/integration owns the source models.
- `openapi.json` is generated from the runtime.
- `index.d.ts` exposes the same contract for TypeScript consumers.
- `version.txt` is the exported contract version used for compatibility checks.
