# Desktop Assets

- `brand/`: daisy marks and brand graphics
- `icons/shell/`: Home/shell navigation icons
- `avatars/`: future avatar art if needed beyond the emoji avatar system
- `fonts/`: local font files when they are added

`home-shell-assets.tsx` is the import boundary for the current Home/shell asset set.
